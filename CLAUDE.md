# CLAUDE.md

This file provides guidance for AI assistants (Claude, Codex, etc.) working in this repository.

## Repository Overview

This is a minimal Git repository (`akifcankilic/test`) used for testing and demonstration purposes. It currently contains a single file: an academic PDF on unsupervised learning.

```
/
└── unimib_unsupervised.pdf   # Academic paper on unsupervised learning (252 KB)
```

## Current State

- **No source code** is present — there is no application, library, or service to build or run.
- **No build system**, package manager, test runner, or CI/CD pipeline is configured.
- **No linting or formatting** configuration exists.

## Git Conventions

- The default branch is `main`.
- Branch naming follows the pattern `<owner>/<description>` (e.g., `codex/ozetle-test-icerigi`, `claude/add-claude-documentation-YYIeT`).
- Commit messages are short and descriptive (e.g., `"Rename test PDF to unimib_unsupervised"`).

## Working in This Repository

Since no source code exists, AI assistants adding code should:

1. **Create a `.gitignore`** appropriate to the language/framework being introduced.
2. **Add a `README.md`** describing the purpose and usage of whatever is added.
3. **Set up a build/test configuration** (e.g., `package.json`, `pyproject.toml`, `Makefile`) before writing source files.
4. **Update this `CLAUDE.md`** to reflect the new structure, conventions, build commands, and test commands.

## Adding New Content

If this repository evolves into a software project, update the sections below:

### Build & Run

```
# Add build/run instructions here
```

### Testing

```
# Add test commands here
```

### Linting & Formatting

```
# Add lint/format commands here
```

## Repository Metadata

| Field       | Value                        |
|-------------|------------------------------|
| Owner       | akifcankilic                 |
| Repo        | test                         |
| Default branch | main                      |
| Last updated | 2026-03-29                  |
